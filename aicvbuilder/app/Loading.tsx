 // this is a loading indicateur , it will automaticly show when we load data on a page

import { Loader2 } from "lucide-react";

 export default function Loading() {
    return <Loader2 className="mx-auto my-6 animate-spin"/>
 }